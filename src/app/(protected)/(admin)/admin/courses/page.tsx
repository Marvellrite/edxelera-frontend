import { RoutePlaceholder } from "@/components/shared/route-placeholder";

export default function AdminCoursesPage() {
  return (
    <RoutePlaceholder
      eyebrow="Admin"
      title="Courses"
      description="Review courses, publishing status, and platform-level course operations."
    />
  );
}
