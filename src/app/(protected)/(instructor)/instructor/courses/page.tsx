import { RoutePlaceholder } from "@/shared/components/shared/route-placeholder";

export default function InstructorCoursesPage() {
  return (
    <RoutePlaceholder
      eyebrow="Instructor"
      title="Courses"
      description="Review instructor-owned courses, drafts, and publishing status."
    />
  );
}
