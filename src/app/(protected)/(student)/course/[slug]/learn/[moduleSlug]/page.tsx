import { RoutePlaceholder } from "@/shared/components/shared/route-placeholder";

export default function CourseModulePage() {
  return (
    <RoutePlaceholder
      eyebrow="Learning"
      title="Module lesson"
      description="Study module content, track progress, and move through course material."
    />
  );
}
