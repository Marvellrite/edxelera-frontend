import { RoutePlaceholder } from "@/shared/components/shared/route-placeholder";

export default function AdminUsersPage() {
  return (
    <RoutePlaceholder
      eyebrow="Admin"
      title="Users"
      description="Manage users, roles, account status, and access controls."
    />
  );
}
