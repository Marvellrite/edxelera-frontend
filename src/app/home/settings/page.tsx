import { RoutePlaceholder } from "@/components/shared/route-placeholder";

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Student"
      title="Settings"
      description="Update notifications, privacy settings, and learning preferences."
    />
  );
}
