import { RoutePlaceholder } from "@/shared/components/shared/route-placeholder";

export default function AddCoursePage() {
  return (
    <RoutePlaceholder
      eyebrow="Instructor"
      title="Add course"
      description="Create course structure, curriculum details, and publishing settings."
    />
  );
}
