import { RoutePlaceholder } from "@/shared/components/shared/route-placeholder";

export default function InstructorPage() {
  return (
    <RoutePlaceholder
      eyebrow="Instructor"
      title="Instructor dashboard"
      description="Manage courses, drafts, learners, and teaching activity."
    />
  );
}
